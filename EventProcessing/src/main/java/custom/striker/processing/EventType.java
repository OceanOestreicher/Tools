package custom.striker.processing;

/**
 * Enum representing the type of an event to be processed by an {@link EventProcessor}
 */
public enum EventType {
    NOTIFICATION,
    REQUEST,
    RESPONSE
}
